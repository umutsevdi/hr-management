# Use postgres/example user/password credentials
version: '3.1'

services:
  db:
    image: postgres
    restart: always
    environment:
      - POSTGRES_PASSWORD=nBdrnL4CVjCaZ8ETXXGyP89CVT4UAw
      - POSTGRES_DB=hr-management
 
    volumes:
      - pgdata:/var/lib/postgresql/data 
    ports:
      - 5432:5432
  adminer:
    image: adminer
    restart: always
    ports:
      - 3000:8080

volumes:
  pgdata:
